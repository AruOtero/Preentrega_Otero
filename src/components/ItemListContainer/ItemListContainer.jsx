/* /* 
const task = new Promise((resuelto,rechazado)=>{ //pendiente-cumplida-rechazada(perdida)//res,rej
  resuelto('toma lo que me prestaste')

})
 */
/* export const ItemListContainer = ({ greeting }) => {

  /* task
  .then(respuestaDelResuelto => console.log(respuestaDelResuelto)) */
 /*  return (
    <div>
      <h2 className="text-center">{ greeting }</h2>
    </div>
  )
} */


export const ItemListContainer = ({ greeting }) => {
  return (
    <div>
      <h2>{ greeting }</h2>
    </div>
  )
}